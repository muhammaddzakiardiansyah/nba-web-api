const validation = (req, res, next) => {
    // const request = {
    //     ...req
    // }
    console.log(req)
    // const {nama, harga, deskripsi} = req.body
    // if(nama && req.file && harga && deskripsi) {
    //     return next()
    // }else {
    //     return res.status(400).send({message: 'semua kolom harus diisi!'})
    // }
    // if(!request.nama) 
    // if(!request.img) return res.status(400).send({message: 'img harus diisi!'})
    // if(!request.deskripsi) return res.status(400).send({message: 'deskripsi harus diisi!'})
    
}

module.exports = validation;